services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: streamly
      POSTGRES_PASSWORD: streamly
      POSTGRES_DB: streamly
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
